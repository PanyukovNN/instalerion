
dependencies {
    implementation rootProject.ext.library.jImageHash

    implementation project(':Common')
}

bootJar {
    archiveVersion = ''
    mainClass = 'org.union.promoter.PromoterApplication'
}

jar.enabled = true
